 <!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <title>Yelper</title>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>

    <!-- Bootstrap -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
    <link rel="stylesheet" href="/assets/style.css">
    <!-- include javascript require tag -->
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
  <nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <%= form_for Food.new, url: random_path, method: :get do |f| %>
        <%= f.submit "Yelper", :type => :image, :src => "/assets/logoHover.png", class: "navbar-brand", id: "yelperLink"%>
      <% end %>
      <ul class="nav navbar-nav">
      <% if @current_user != nil%>
      <li class="active"><%= link_to 'My Account', user_path(@current_user), id:"myAccount" %><span class="sr-only">(current)</span></li>
      <% end %>
      </ul>
      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav navbar-right col-xs-5">
        <!-- <ul class="nav navbar-nav"> -->
          <!-- Change users_path to user_path[id] once merged -->
        <!-- </ul> -->
          <div  class="navbar-form navbar-left col-xs-1" >
            <%= form_for Food.new, url: food_search_path, method: :get do |f| %>
              <%= f.autocomplete_field :name, autocomplete_search_query_foods_path, class: "form-control", style: "width: 340px;", placeholder: "Enter Food Search" %>
              <%= f.submit "search", class: "btn btn-default" %>
            <% end %>
          </div>
          <!-- if user is logged in  -->
          <% if @current_user != nil%>
          <li><%= link_to 'Logout', logout_path, method: :delete, class: "navbar-links" %></li>
          <% end %>
         <!--  else -->
          <% if @current_user == nil%>
          <li><%= link_to 'Login', login_path, class: "navbar-links" %></li>
          <li><%= link_to 'Signup', signup_path, class: "navbar-links" %></li>
          <% end %>
          <!-- end -->
        </ul>
      </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
  </nav>
        </div>
  </div>
</nav>
<div class="row">
  <div class="col-md-10 col-md-offset-1 content-container">
    <!-- render errors if any -->
    <%= render 'shared/flash' %>
    <%= yield %>
  </body>
  </div>
  </div>
  <nav class="navbar navbar-default navbar-fixed-bottom">
    <div class="container-fluid">
      <div class="navbar-header">
        <a class="navbar-brand" href="http://www.yelp.com" target="_blank">
          <img alt="Brand" src="https://s3-media2.fl.yelpcdn.com/assets/srv0/developer_pages/55e2efe681ed/assets/img/yelp_logo_50x25.png">
        </a>
      </div>
    </div>
  </nav>
</html>
